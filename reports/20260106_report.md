# Report 6.1.2026


## Achievements


We have created a solver structure which uses an adapter for the solvers. After coding the adapters for gurobi and cpsat we have less duplicate code for the model. The modules and variables have been adjusted to use the adapter interface, such that they can be used by the different solvers. Additionally, we defined a simple solver interface and implemented the gurobi solver.
We also implemented most of the modules from the mentioned paper and their corresponding validation functions.
We are still trying to find a way to accommodate this concept to hexaly since hexaly uses a context manager for the solver class.
Finally, we implemented more modules from the paper and updated the project structure 


## Schedule


### Meeting 
Over the holidays we have not been able to meet. To catch up and discuss our further procedure (like the to-dos), we will have an online meeting on Friday.


### To-dos
- hexaly implementation, 
- backend functions for the solvers, 
- conceptualize frontend (UI, functions),
- benchmarking


### Bennet
I was able to implement the missing modules and the corresponding validation functions but i have not tested them yet thoroughly.


### Eike
In collaboration with Bennet i brainstormed on the implementation and code  of the missing modules. So far the plan for the upcoming days is to test more and write the code for the solvers. Once that is done and the next meeting reveals our next needs that plan might be adjusted.


### Simon
Since I wasn't able to get my Laptop through airport security, I did not achieve everything I wanted to. I did some research regarding Hexaly and looked into solutions for the different structure of the solver.
Next week I will focus on implementing a solution. 


### Valentin
Fortunately, I was able to achieve all the goals I had set myself, and a bit more. With the new adapter structure, the tests do not work anymore and have to be adjusted, with certain tools needing to be rewritten. That is what I plan to do.
